<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fusion.kim.m_reproductivehealth">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".AntenatalCareActivity" />
        <activity android:name=".GynacologyActivity" />
        <activity android:name=".FamilyPlanningActivity" />
        <activity android:name=".HospitalSelectionActivity" />
        <activity
            android:name=".PortalActivity"
            android:label="@string/title_activity_portal"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SchedulingActivity" />
        <activity android:name=".EditRegionActivity" />
        <activity android:name=".PrevisitFormsActivity" />

        <!--
        <service
            android:name=".MyFirebaseMessagingService"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name=".FirebaseIDService"
            android:enabled="true"
            android:exported="true"></service>
        -->
        <activity android:name=".FetalUpdatesActivity" />
        <activity android:name=".PrescriptionActivity" />
        <activity android:name=".HealthInformationActivity" />
        <activity android:name=".HelpActivity"></activity>
    </application>

</manifest>